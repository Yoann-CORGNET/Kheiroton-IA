services:
  backend:
    build: ./backend
    ports:
      - "8002:8000"
    volumes:
      - ./data_agreg:/data_agreg:ro
    restart: unless-stopped

  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend
    restart: unless-stopped
